1.修复部分Bug，如没有自动创建profilePath目录、更新部分逻辑，优化API的调用、代码更加typescript化。
2.去除无用的echarts画图
3.修改|增加隧道界面，可以选择输入端口和当前系统的已有端口(显示进程名称、PID、协议、端口等)
4.增加系统通知的模式，调试模式和用户模式
5.增加去除敏感信息功能、比如token等、分为写入模式(包括读取模式, 写入文件时擦除敏感信息、
读取时也去除敏感信息)和读取方式(写入文件中带有token、读取时屏蔽)
6.保存登录模式、点击刷新token，则会使用保存的用户名和密码进行重新登录(用户名和密码存储在文件中会进行加密)
